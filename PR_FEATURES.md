# PR 가져오기 및 리뷰 파일 열기 기능 동작 사항

## 1. PR 가져오기 (`fetchPRInfo`)

### 위치
- Review 모드 → PR 번호 입력 → "PR 가져오기" 버튼 클릭

### 동작 흐름
1. **PR 번호 입력**
   - PR 번호가 없으면 입력창 표시
   - 유효성 검증 (양수만 허용)

2. **PR 상세 정보 가져오기**
   - GitHub API를 통해 PR 정보 조회
   - PR이 없거나 접근 불가 시 에러 메시지 표시

3. **PR 브랜치로 체크아웃**
   - 브랜치명: `pr-{번호}/{제목-slug}`
   - 로컬에 브랜치가 이미 있으면 체크아웃 후 최신 업데이트
   - 없으면 새로 생성

4. **PR의 변경된 파일 열기**
   - PR에 포함된 모든 마크다운 파일 (`.md`) 필터링
   - 각 파일을 단독으로 열기
   - **영문 파일과 함께 Split View로 열지 않음**
   - 번역 파일, 영문 파일, 기타 파일 모두 동일하게 단독으로 열기

5. **완료 메시지 표시**
   - 열린 파일 수, PR 정보 (번호, 제목, 작성자, 상태) 표시

### 특징
- ✅ PR의 모든 마크다운 파일을 자동으로 열어줌
- ✅ 영문 파일과 함께 열지 않음 (단독으로만 열림)
- ✅ 파일 열기 실패 시 에러 로그만 남기고 계속 진행

---

## 2. 리뷰 파일 열기 (`openReviewFile`)

### 위치
- Review 모드 → "리뷰 파일 가져오기" 버튼 클릭
- 단축키: `Cmd+Shift+R` (Mac) / `Alt+Shift+R` (Windows/Linux)

### 동작 흐름
1. **현재 활성화된 파일 확인**
   - 활성 에디터가 없으면 안내 메시지 표시 후 종료

2. **파일 타입 확인**
   - 절대 경로와 상대 경로 모두 확인
   - `content/{언어}/` 패턴으로 번역 파일 여부 판단

3. **번역 파일인 경우**
   - ✅ 영문 원본 경로 찾기 (`content/{언어}/` → `content/en/`)
   - ✅ 영문 파일 존재 여부 확인 (파일 시스템에서 확인)
   - ✅ 영문 파일이 있으면 → **Split View로 열기** (영문 왼쪽, 번역 오른쪽)
   - ❌ 영문 파일이 없으면 → 안내 메시지 표시 후 종료

4. **영문 파일이거나 기타 파일인 경우**
   - ❌ 안내 메시지 표시 후 종료
   - 메시지: "현재 파일이 번역 파일이 아닙니다. 번역 파일(content/{언어}/)을 열어주세요."

### 특징
- ✅ 현재 열려있는 번역 파일만 처리
- ✅ 영문 파일과 함께 Split View로 열기
- ✅ 번역 파일이 아니면 안내 메시지 표시
- ✅ 영문 파일이 없으면 안내 메시지 표시

---

## 주요 차이점

| 기능 | 파일 열기 방식 | 용도 |
|------|--------------|------|
| **PR 가져오기** | 모든 변경 파일을 단독으로 열기 | PR 체크아웃 및 변경 파일 확인 |
| **리뷰 파일 열기** | 현재 번역 파일의 영문 파일과 함께 Split View로 열기 | 번역 리뷰 작업 |

---

## 사용 시나리오

### PR 리뷰 시나리오
1. Review 모드로 전환
2. PR 번호 입력 후 "PR 가져오기" 클릭
   - → PR 체크아웃 + 모든 변경 파일 열기
3. 번역 파일에서 "리뷰 파일 가져오기" 클릭
   - → 영문 파일과 함께 Split View로 열기
4. 스크롤 동기화 활성화
5. 번역 리뷰 진행

---

## 기술적 세부사항

### 파일 경로 처리
- 절대 경로와 상대 경로 모두 지원
- `content/{언어}/` 패턴으로 번역 파일 감지
- `content/en/` 패턴으로 영문 파일 감지
- 대소문자 구분 없이 처리

### 에러 처리
- 파일 열기 실패 시 에러 로그만 남기고 계속 진행
- 영문 파일이 없으면 안내 메시지 표시
- 번역 파일이 아니면 안내 메시지 표시

